1.NumPy矢量化运算比Python循环高效的原因：
底层由C语言实现，避免了Python解释器的逐元素开销；
数组数据在内存中连续存储，利用CPU缓存和SIMD指令加速；
矢量化操作一次性完成，减少Python层级循环和类型检查。

2.reshape与resize的区别：
reshape：返回新视图（若可能）或副本，不改变原数组，且要求新形状元素总数与原数组相同。
resize：可以改变原数组大小，若新元素总数大于原数组，则用重复值（或0）填充；若小于则截断。
np.resize返回新数组，而ndarray.resize会原地修改。